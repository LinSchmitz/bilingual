import React from 'react';

export function Footer({ t }) {
  return (
    <footer>
      <p>{t.footerTeam}</p>
      <p>{t.footerContact}</p>
      <p>{t.footerResource}</p>
      <p>{t.footerRes} </p>
    </footer>
  );
}
